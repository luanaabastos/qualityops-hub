{
  "name": "@qualityops-hub/api",
  "description": "Authenticated report ingestion and PostgreSQL persistence for the QualityOps Hub demo.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "migrate:deploy": "node dist/migrate.js",
    "bootstrap:demo": "node dist/bootstrap.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/static": "10.1.2",
    "@qualityops-hub/shared": "workspace:*",
    "fastify": "^5.1.0",
    "pg": "8.23.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "8.21.0",
    "eslint": "^9.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
